import ThemedText from '@/components/global/themedText'
import ThemedView from '@/components/global/themedView'

const AddMediaItem = () => {
  return (
    <ThemedView>
      <ThemedText>This will be the add media item page</ThemedText>
    </ThemedView>
  )
}

export default AddMediaItem
